# Proyecto para la validación de DTO's

En este proyecto haremos la validación objetos DTO, haciendo la verificación de campos requeridos y el mapper desde una entidad DB a un DTO, a través de un archivo de propiedades

* Clase de validación de funcionalidad:
	com.obj.test.Test